# Dashboard module (optional)

This folder contains a UI for viewing device telemetry.

## Recommended first milestone

- Show latest reading values.
- Add a simple table of recent data.
- Handle API loading/error states.

A command-line viewer is also acceptable if you want to postpone frontend work.
