# Firmware module

This folder contains microcontroller code.

## Recommended first milestone

- Read one sensor value every 10 seconds.
- Print values over serial for local verification.
- Add Wi-Fi connectivity and POST to backend.

## Keep in mind

- Never hardcode secrets in source files.
- Add simple retry/backoff for network failures.
- Log errors in a way that is visible over serial.
